"use strict";
/* Famous Quote 2: Repeat Exercise 4, but this time store the famous persons name in a variable called
famous_person. Then compose your message and store it in a new variable called message. print your message */
let famous_person = "Muhammad Ali Jinnah";
let quote = "Think a hundred times before you take a decision, but once that decision is taken, stant by it as one man.";
let message = `${famous_person} once said, "${quote}"`;
console.log(message);
